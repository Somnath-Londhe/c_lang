#include<stdio.h>
int main(){
    int marks[4];
    for (int i = 0; i < 4; i++)
    {
        printf("enter the value of %d  in the arr \n ",i);
        scanf("%d",&marks[i]);
    }
    
    // marks[0]=12;
    // printf("this is the number %d of arr \n",marks[0]);
    // marks[0]=14;
    //  printf("this is the number %d of arr \n",marks[0]);
    //  marks[0]=56;
    //   printf("this is the number %d of arr \n",marks[0]);
}